language: cpp
compiler: gcc-4.9
dist: trusty

before_install:
  - sudo apt-get install -y
      gcc-4.9
      automake
      autoconf
      autoconf-archive
      libtool
      liblz4-dev
      liblzma-dev
      make
      zlib1g-dev
      binutils-dev
      libjemalloc-dev
      libiberty-dev
      libjsoncpp-dev
  - sudo sh get_boost.sh

script: autoreconf -ivf && ./configure CXX=gcc-4.9 && make -j4
